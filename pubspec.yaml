name: vision_app
description: "Vision AI App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  flutter_markdown: ^0.6.14
  google_generative_ai: ^0.4.4  # Kralların paketi geri geldi
  flutter_riverpod: ^3.0.3
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  state_notifier: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
